package com.gblrod.agentsvault.language

import com.gblrod.agentsvault.R

enum class LanguageOptions(
    val label: Int
) {
    PT_BR(label = R.string.language_portuguese),
    EN_US(label = R.string.language_english),
    ES(label = R.string.language_espanhol)
}